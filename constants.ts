import { Gender, Goal, Level, Location, DietType } from "./types";

export const GENDER_OPTIONS = Object.values(Gender);
export const GOAL_OPTIONS = Object.values(Goal);
export const LEVEL_OPTIONS = Object.values(Level);
export const LOCATION_OPTIONS = Object.values(Location);
export const DIET_OPTIONS = Object.values(DietType);
